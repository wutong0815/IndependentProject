<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootmedical.Repository.PatientRepository">

    <select id="selectPatientPage" resultType="com.example.springbootmedical.entity.Patient">
        SELECT * FROM patient
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="medicalRecordNumber != null and medicalRecordNumber != ''">
            AND medical_record_number LIKE CONCAT('%', #{medicalRecordNumber}, '%')
        </if>
        <if test="contactPhone != null and contactPhone != ''">
            AND contact_phone LIKE CONCAT('%', #{contactPhone}, '%')
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="findByMedicalRecordNumber" resultType="com.example.springbootmedical.entity.Patient">
        SELECT * FROM patient
        WHERE medical_record_number = #{medicalRecordNumber} AND deleted = 0
    </select>

</mapper>