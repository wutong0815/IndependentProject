<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootmedical.Repository.MedicalDeviceRepository">
    <select id="selectDevicePage" resultType="com.example.springbootmedical.entity.MedicalDevice">
        SELECT * FROM medical_device
        WHERE deleted = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="model != null and model != ''">
            AND model LIKE CONCAT('%', #{model}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="departmentId != null">
            AND department_id = #{departmentId}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="findBySerialNumber" resultType="com.example.springbootmedical.entity.MedicalDevice">
        SELECT * FROM medical_device
        WHERE serial_number = #{serialNumber} AND deleted = 0
    </select>

</mapper>