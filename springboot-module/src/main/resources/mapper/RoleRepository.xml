<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootmodule.mapper.RoleRepository">

    <select id="selectRolePage" resultType="com.example.springbootmodule.domain.Role">
        SELECT * FROM sys_role
        WHERE deleted = 0
        <if test="roleName != null and roleName != ''">
            AND role_name LIKE CONCAT('%', #{roleName}, '%')
        </if>
        <if test="roleCode != null and roleCode != ''">
            AND role_code LIKE CONCAT('%', #{roleCode}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="findByRoleCode" resultType="com.example.springbootmodule.domain.Role">
        SELECT * FROM sys_role
        WHERE role_code = #{roleCode} AND deleted = 0
    </select>

    <select id="findRolesByUserId" resultType="com.example.springbootmodule.domain.Role">
        SELECT r.* FROM sys_role r
                            INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND ur.deleted = 0
        ORDER BY r.create_time DESC
    </select>

</mapper>